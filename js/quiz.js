const Quiz = (() => {function normalize(v){return String(v||'').trim().replace(/\s+/g,' ').toLowerCase()}function check(exercise,value){if(exercise.type==='choice'||exercise.type==='fill')return normalize(value)===normalize(exercise.answer);const text=normalize(value);return (exercise.keywords||[]).every(k=>text.includes(normalize(k)))}function feedback(exercise,correct){if(correct)return `Muy bien: ${exercise.explanation}`;return `Todavía no. Pista: ${exercise.hint} Explicación sencilla: ${exercise.explanation}` }return{check,feedback}})();
